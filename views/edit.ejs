<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chỉnh sửa người dùng</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Chỉnh sửa thông tin người dùng</h1>
        <form action="/edit/<%= user._id %>" method="POST">
            <label for="name">Tên:</label>
            <input type="text" name="name" id="name" value="<%= user.name %>" required>
            
            <label for="email">Email:</label>
            <input type="email" name="email" id="email" value="<%= user.email %>" required>
            
            <label for="age">Tuổi:</label>
            <input type="number" name="age" id="age" value="<%= user.age %>" required>
            
            <label for="address">Địa chỉ:</label>
            <input type="text" name="address" id="address" value="<%= user.address %>" required>

            <button type="submit">Cập nhật</button>
        </form>
        <a href="/">Quay lại danh sách người dùng</a>
    </div>
</body>
</html>